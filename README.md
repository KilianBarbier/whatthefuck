# whatthefuck.fr 🤯

Un bouton **WTF** qui affiche une image au hasard. Page statique, sans dépendance.

## Lancer en local
Ouvre simplement `index.html` dans un navigateur.

## Fichiers
- `index.html` — la page
- `style.css` — le style
- `script.js` — la logique du bouton (images SVG intégrées)
